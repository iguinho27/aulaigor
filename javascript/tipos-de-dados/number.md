# Valores do tipo de dados number

O tipo de dados number em JavaScript representa valores numéricos, tanto inteiros quanto de ponto flutuante. Ele é usado para realizar operações matemáticas e cálculos.

```javascript
let inteiro = 42; // Número inteiro
let pontoFlutuante = 3.14; // Número de ponto flutuante
```